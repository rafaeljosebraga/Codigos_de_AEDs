#include <stdio.h>
#include <stdlib.h>
typedef struct {
	int peso;
	int valor;
}elemento;
int bigger(int a,int b){
	return (a>b) ? a:b;
}

int main(){
//Variables
	int elem_quant;
	int peso_max;
	int i,w;//contador
//Receive Data
	scanf("%d %d",&elem_quant,&peso_max);

}
